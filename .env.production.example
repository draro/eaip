# MongoDB Atlas Configuration
# Get this from MongoDB Atlas: Connect -> Drivers -> Connection String
MONGODB_URI=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/eaip?retryWrites=true&w=majority

# NextAuth Configuration
# IMPORTANT: Set this to your actual domain or VPS IP
NEXTAUTH_URL=https://yourdomain.com
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=your-super-secret-key-minimum-32-characters

# AI Provider Configuration
# You need at least one of these
ANTHROPIC_API_KEY=sk-ant-api03-xxxxx
OPENAI_API_KEY=sk-xxxxx

# AWS S3 Configuration (Optional - only if using S3 for file storage)
# Leave empty if not using S3
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=us-east-1
S3_BUCKET_NAME=

# Email Configuration (Optional - only if using email notifications)
# Leave empty if not using email
EMAIL_SERVER=
EMAIL_FROM=

# Domain Configuration
# Set this to your VPS public IP address
EAIP_TARGET_IP=123.456.789.012

# Node Environment
NODE_ENV=production
